import 'package:flutter/material.dart';

class RecordedListGlucoseScreen extends StatelessWidget {
  const RecordedListGlucoseScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('혈당기록 리스트 화면'),
    );
  }
}
